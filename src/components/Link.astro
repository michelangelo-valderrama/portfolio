---
import type { HTMLAttributes } from "astro/types"

interface Props extends HTMLAttributes<"a"> {}
---

<a
  {...Astro.props}
  class="underline underline-offset-2 hover:text-accent-foreground transition"
  ><slot /></a
>
